/** Display helpers from the services catalog page, reused outside `features/services`. */
export {
  formatDuration,
  formatPrice as formatPriceUtil,
  getImageUrl,
  getStatusColor,
} from "@/features/services/pages/ServicesPage/utils";
