/**
 * Redux store instance and dispatch types.
 * Bootstrap lives in `index.ts`; this file re-exports the same API for imports from `@/app/store/store` if needed.
 */
export { store, type RootState, type AppDispatch } from "./index";
