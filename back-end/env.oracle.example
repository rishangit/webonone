# Oracle Cloud Infrastructure (OCI) MySQL Database Configuration Template
# Copy this file to .env.oracle and fill in your actual values
# This file is safe to commit to version control (no real credentials)

# Database Configuration
# Get the endpoint from: OCI Console > Databases > MySQL > Your DB System > Endpoints
# For Always Free tier, the endpoint format is typically:
# - Private endpoint: An IP address in your VCN
# - Public endpoint: <db-system-name>.<region>.mysql.database.oraclecloud.com
DB_HOST=your-mysql-endpoint.mysql.uk-london-1.oci.oraclecloud.com
DB_PORT=3306
DB_USER=admin
DB_PASSWORD=your-secure-password
DB_NAME=appapp

# Server Configuration
PORT=5007
NODE_ENV=production

# JWT Configuration
# Generate a secure secret: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-use-strong-random-string
JWT_EXPIRES_IN=7d

# CORS Configuration
# Update with your actual frontend domain
FRONTEND_URL=https://your-frontend-domain.com

# Rate Limiting (Production settings)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=2000

# Google Maps API
GOOGLE_MAPS_API_KEY=your-google-maps-api-key
